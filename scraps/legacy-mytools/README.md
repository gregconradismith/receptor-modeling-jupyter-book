# Legacy MyTools notebooks

These notebooks use the older `MyTools.ipynb` helper notebook and CamelCase
filenames. The current book uses `receptor_tools.ipynb` and the `tools_and_examples_*`
notebooks instead. These files were not listed in `_toc.yml` when archived.
