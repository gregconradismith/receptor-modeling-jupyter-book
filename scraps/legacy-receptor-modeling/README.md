# Legacy receptor modeling drafts

These pages were archived from the repository root because they duplicate material
that now appears in the active Jupyter Book table of contents.

The published book uses `only_build_toc_files: true` in `_config.yml`, and these
files were not listed in `_toc.yml` before being moved here. The move keeps the
draft material available without mixing it with the current source pages.
